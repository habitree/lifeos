# 배포 아티팩트 오류 해결
## GitHub Actions 배포 아티팩트 오류 분석 및 해결

**작성일**: 2025-01-27

---

## 🚨 발견된 오류

### 오류 메시지

```
Unable to download artifact(s): Artifact not found for name: build-artifacts

Please ensure that your artifact is not expired and the artifact was uploaded using a compatible version of toolkit/upload-artifact.
```

### 원인 분석

1. **아티팩트가 업로드되지 않음**
   - 빌드 job이 실패했을 수 있음
   - 아티팩트 업로드 단계가 실행되지 않았을 수 있음

2. **아티팩트 이름 불일치**
   - 업로드와 다운로드에서 다른 이름 사용
   - 대소문자 불일치

3. **아티팩트 액션 버전 호환성**
   - `upload-artifact@v4`와 `download-artifact@v4` 버전 불일치
   - 아티팩트 만료

4. **Next.js 빌드 아티팩트 전달 문제**
   - Vercel은 자체 빌드를 수행하므로 `.next` 폴더 전달이 불필요할 수 있음
   - 아티팩트 크기 제한 초과 가능성

---

## ✅ 해결 방법

### 방법 1: 아티팩트 사용 제거 (권장)

Vercel은 자체 빌드를 수행하므로, 빌드 아티팩트를 전달할 필요가 없습니다.

**장점**:
- ✅ 더 간단한 워크플로우
- ✅ 아티팩트 관련 오류 방지
- ✅ Vercel이 최적화된 빌드 수행

**변경 사항**:
- 빌드 job에서 아티팩트 업로드 제거
- 배포 job에서 아티팩트 다운로드 제거
- Vercel이 소스 코드에서 직접 빌드 수행

---

### 방법 2: 아티팩트 액션 버전 확인

아티팩트 업로드/다운로드 액션 버전을 최신으로 업데이트:

```yaml
- uses: actions/upload-artifact@v4
- uses: actions/download-artifact@v4
```

---

### 방법 3: 빌드 job 의존성 확인

배포 job이 빌드 job의 성공을 기다리도록 설정:

```yaml
deploy:
  needs: build
  if: success()  # 빌드가 성공한 경우에만 실행
```

---

## 🔧 권장 수정 사항

### 수정된 워크플로우 구조

1. **빌드 job**: 빌드 및 테스트만 수행 (아티팩트 업로드 제거)
2. **배포 job**: Vercel이 소스 코드에서 직접 빌드 수행

이렇게 하면:
- ✅ 아티팩트 관련 오류 방지
- ✅ 더 빠른 배포
- ✅ Vercel의 최적화된 빌드 활용

---

## 📝 수정된 워크플로우 예시

```yaml
jobs:
  build:
    # 빌드 및 테스트만 수행
    # 아티팩트 업로드 제거
  
  deploy:
    needs: build
    # 아티팩트 다운로드 제거
    # Vercel이 소스 코드에서 직접 빌드 수행
```

---

## 🔍 문제 해결 체크리스트

- [ ] 빌드 job이 성공했는지 확인
- [ ] 아티팩트 업로드 단계가 실행되었는지 확인
- [ ] 아티팩트 이름이 일치하는지 확인
- [ ] 아티팩트 액션 버전이 호환되는지 확인
- [ ] 아티팩트 만료 여부 확인

---

## 📚 참고 자료

- [GitHub Artifacts FAQ](https://github.com/actions/toolkit/blob/main/packages/artifact/docs/faq.md)
- [Vercel GitHub Actions](https://vercel.com/docs/concepts/git/vercel-for-github)
- [GitHub Actions Artifacts](https://docs.github.com/en/actions/using-workflows/storing-workflow-data-as-artifacts)

---

> **LIFE OS v0.1**  
> 기준으로 돌아오는 루틴 앱

